import React from 'react'

export default function Start({ onClick }) {
  return (
    <div className='font-bold bg-[#8F7A66] text-[#fff] py-2 px-5 rounded-md cursor-pointer' onClick={onClick}>New Game</div>
  )
}
